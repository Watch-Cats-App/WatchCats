version: '3'

services:
  web:
    image: cr.yandex/crped8mo5jtkn5mldmbq/cat-server:cat-server
    ports:
      - "80:80"
    depends_on:
      - db
  db:
    image: postgres:14.1-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=SDLFKJGBAiuev1673f
      - POSTGRES_DB=cat_db
volumes:
  postgres_data:
